#!/usr/bin/env bash

set -u
set -e

ARGUMENT=${1:-''}
YOUTUBE_PLAYLIST_ID=PLOoislNmW3OiDeQrbVYqBG0AZ-NdhH2D1

# Stop script when running in background
if [ "$ARGUMENT" == "stop" ]; then
    pkill -f mpsyt
    exit 0
fi

nohup mpsyt //$YOUTUBE_PLAYLIST_ID, 1, shuffle, all &>/dev/null &
