#!/usr/bin/env bash

set -u
set -e

ARGUMENT=${1:-''}
SOUND=MgApT3VHtZY

# Stop script when running in background
if [ "$ARGUMENT" == "stop" ]; then
    pkill -f mpsyt
    exit 0
fi

nohup mpsyt playurl $SOUND &>/dev/null &
